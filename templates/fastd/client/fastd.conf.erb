log level error;
log to syslog level debug;

interface "ffks-client";
method "salsa2012+gmac";
bind <%= @ip_addr %>:10001;
include "../secret.conf";
mtu 1426;

include peer "peers/vpn1" as "vpn1";

on up "
  ip link set up dev $INTERFACE
";
